#include <stdio.h>

int main(){
    int lang =1;
    if (lang == 1)
    printf("Hello world!");
    else
    printf("안녕하세요!");
return 0;

}
//------powershell--------./hello 로 실행하라.
//같은 언어라도 운영체제에 따라 달라진다.
//cpu와 운영체제 두개궁합이 맞아야 프로그램이 실행된다.
//보통 리눅스는 확장자를 안붙임. hello.c -> hellㅔ
//자바 버추얼 머신은 JRE 에 있따.+ 컴파일러, 디버거
//자바 개발자는 운영체제 신경쓰지말어 ㅋ
//JIT AOT 